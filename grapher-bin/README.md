# Grapher-Bin

This is a demonstration of WasmGrapher running native (i.e. without WebAssembly).

Compile this crate by running `cargo build -p grapher-bin --release --target "x86_64-unknown-linux-gnu"`.

Run the program by running `./target/x86_64-unknown-linux-gnu/release/grapher-bin`.
